# OpenBehavior connected to Simulator and Autonomous Vehicle

We can achieve connectivity between OpenBehavior and various simulators as well as different autonomous driving systems by flexibly configuring adapters. For example, in our work, we enable testing of Apollo within the CARLA simulator via OpenBehavior by configuring an adapter.